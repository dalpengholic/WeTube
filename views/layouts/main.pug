doctype html
html
    head
        script(src="https://kit.fontawesome.com/abe311f4c2.js", crossorigin="anonymous")
        title #{pageTitle} | #{siteName}
    body
        include ../partials/header
        main
            block content
        include ../partials/footer